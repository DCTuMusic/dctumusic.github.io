---
interface Props {
	text?: string;
}

const { text = '' } = Astro.props;
---

{
	text && (
		<section class="mx-auto max-w-4xl px-6 py-20 text-center md:px-10 md:py-24">
			<p class="font-display text-2xl leading-snug text-paper sm:text-3xl md:text-4xl">{text}</p>
		</section>
	)
}
